#include<bits/stdc++.h>

using namespace std;
typedef long long ll;
#define REP(i,n) for(int i=0;i<(int)(n);i++)

int main(){
    ll x;
    cin >> x;
    cout << (ll)std::pow(x,3) << endl;
    return 0;
}