#include<bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef unsigned long long ull;
#define REP(i,n) for(int i=0;i<(int)(n);i++)

int main(){
    ll n,k;
    cin>>n>>k;
    if(n<=k)cout<<"1"<<endl;
    else cout<<"0"<<endl;
    return 0;
}